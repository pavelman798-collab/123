# Руководство по SMS-кампаниям 💬

## 🎯 Что добавлено:

### 1. Типы кампаний

Теперь при создании кампании можно выбрать один из трёх типов:

| Тип | Описание | Использование |
|-----|----------|---------------|
| **📞 Позвонить** | Только звонки | Голосовые опросы, напоминания |
| **💬 Отправить СМС** | Только СМС | Массовые уведомления, рассылки |
| **📞💬 Позвонить + СМС** | Звонок, затем СМС | Многоуровневое взаимодействие |

### 2. Сценарии отправки СМС

Для типа **"Позвонить + СМС"** доступны гибкие настройки:

- ❌ **СМС при недозвоне** - если абонент не ответил
- ✅ **СМС при успешном звонке** - после разговора

### 3. Управление шаблонами

- 💾 **Сохранение шаблонов** - сохраняйте часто используемые тексты
- 📋 **Библиотека шаблонов** - загружайте сохраненные шаблоны одним кликом
- 🏷️ **Категории** - шаблоны разделены по категориям (при недозвоне / при успехе)

---

## 🚀 Как использовать:

### Создание SMS-кампании

#### Вариант 1: Только СМС

1. **Откройте "Новая кампания"**
2. **Выберите тип:** 💬 Отправить СМС
3. **Введите текст СМС** или выберите шаблон
4. **Загрузите CSV** с номерами
5. **Запустите кампанию**

#### Вариант 2: Звонки + СМС при недозвоне

1. **Выберите тип:** 📞💬 Позвонить + Отправить СМС
2. **Выберите аудиофайл** для звонка
3. **Отметьте:** ☑️ Отправлять СМС при недозвоне
4. **Введите текст СМС** для недозвона
5. **Запустите кампанию**

**Результат:** Система позвонит → если не ответили → отправит СМС

#### Вариант 3: Звонки + СМС после разговора

1. **Выберите тип:** 📞💬 Позвонить + Отправить СМС
2. **Выберите аудиофайл** для звонка
3. **Отметьте:** ☑️ Отправлять СМС при успешном дозвоне
4. **Введите текст СМС** (например, ссылка на сайт)
5. **Запустите кампанию**

**Результат:** Система позвонит → если ответили → отправит СМС

---

## 📝 Примеры использования:

### Пример 1: Напоминание о записи к врачу

```
Тип: Позвонить + СМС
Аудио: reminder_doctor.wav ("Напоминаем о вашей записи...")

✅ СМС при недозвоне:
"Здравствуйте! Напоминаем о записи к врачу завтра в 15:00.
Клиника 'Здоровье', ул. Ленина 10. Тел: 8-800-123-45-67"
```

**Логика:** Позвонили → не ответили → отправили СМС с информацией

### Пример 2: Акция в магазине

```
Тип: Позвонить + СМС
Аудио: sale_announcement.wav ("Только сегодня скидка 50%...")

✅ СМС при успешном дозвоне:
"Скидка 50% на все! Код: SALE50
Покажите это СМС на кассе.
Магазин работает до 21:00.
Адрес: ТЦ 'Мега', 2 этаж"
```

**Логика:** Позвонили → ответили и прослушали → отправили СМС с промокодом

### Пример 3: Массовая SMS-рассылка

```
Тип: Отправить СМС

Текст СМС:
"Уважаемый клиент! Ваш заказ №12345 готов к получению.
Пункт выдачи: ул. Пушкина 5
Работаем: 9:00-21:00
Срок хранения: 3 дня"
```

**Логика:** Только отправка СМС без звонков

### Пример 4: Подтверждение заказа

```
Тип: Позвонить + СМС (оба сценария)

Аудио: order_confirmation.wav

✅ СМС при недозвоне:
"Ваш заказ №777 принят! Доставка завтра 10:00-14:00.
Перезвоните для уточнения: 8-800-100-20-30"

✅ СМС при успешном дозвоне:
"Спасибо за подтверждение заказа №777!
Отследить доставку: https://shop.ru/track/777
Приятных покупок!"
```

**Логика:**
- Не ответили → СМС с просьбой перезвонить
- Ответили → СМС с ссылкой на трекинг

---

## 🛠️ Работа с шаблонами:

### Сохранение шаблона

1. **Введите текст СМС** в поле
2. **Нажмите** ➕ Сохранить как шаблон
3. **Введите название** (например: "Напоминание о записи")
4. **Готово!** Шаблон сохранен

### Использование шаблона

1. **Нажмите** 🔄 рядом с выбором шаблона
2. **Выберите шаблон** из списка
3. **Текст автоматически** вставится в поле
4. **При необходимости отредактируйте** текст

### Категории шаблонов

- **no_answer** - для недозвона
- **success** - для успешного звонка
- **general** - общие шаблоны

---

## 🔧 Обновление системы:

### Шаг 1: Остановите контейнеры

```bash
cd ~/Desktop/docker
docker compose stop
```

### Шаг 2: Обновите базу данных

```bash
# Пересоздайте базу данных с новой схемой
docker compose down postgres
docker volume rm docker_postgres_data  # ВНИМАНИЕ: удалит все данные!
docker compose up -d postgres
```

**⚠️ ВАЖНО:** Эта команда удалит все существующие кампании! Сделайте backup если нужно:

```bash
# Backup (до удаления)
docker exec phone-postgres pg_dump -U phone_user phone_campaigns > backup.sql

# Restore (после пересоздания)
cat backup.sql | docker exec -i phone-postgres psql -U phone_user phone_campaigns
```

### Шаг 3: Перезапустите campaign-manager

```bash
docker compose restart campaign-manager
docker compose logs -f campaign-manager
```

**Ожидаемый вывод:**
```
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Шаг 4: Проверьте работу

Откройте http://localhost:8000 и создайте тестовую SMS-кампанию!

---

## 📊 API Примеры:

### Создание SMS-кампании

```bash
curl -X POST http://localhost:8000/api/campaigns \
  -H "Content-Type: application/json" \
  -d '{
    "name": "SMS рассылка",
    "campaign_type": "sms",
    "sms_on_no_answer": "Ваше СМС сообщение",
    "send_sms_on_no_answer": false,
    "send_sms_on_success": false
  }'
```

### Создание комбинированной кампании

```bash
curl -X POST http://localhost:8000/api/campaigns \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Звонок + SMS",
    "campaign_type": "call_and_sms",
    "audio_file": "message.wav",
    "sms_on_no_answer": "Не удалось дозвониться. Перезвоните нам!",
    "sms_on_success": "Спасибо за звонок! Подробности: http://site.ru",
    "send_sms_on_no_answer": true,
    "send_sms_on_success": true
  }'
```

### Сохранение шаблона

```bash
curl -X POST http://localhost:8000/api/sms/templates \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Напоминание о записи",
    "text": "Здравствуйте! Напоминаем о записи завтра в 15:00.",
    "category": "no_answer"
  }'
```

### Получение списка шаблонов

```bash
# Все шаблоны
curl http://localhost:8000/api/sms/templates

# Только для недозвона
curl http://localhost:8000/api/sms/templates?category=no_answer

# Только для успешного звонка
curl http://localhost:8000/api/sms/templates?category=success
```

### Отправка одиночного SMS

```bash
curl -X POST http://localhost:8000/api/sms/send \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "79991234567",
    "message": "Тестовое СМС",
    "sim_number": 1
  }'
```

---

## ⚙️ Настройка GoIP:

### Переменные окружения

Добавьте в `docker-compose.yml`:

```yaml
campaign-manager:
  environment:
    - GOIP_HOST=192.168.8.1      # IP GoIP устройства
    - GOIP_USER=admin             # Логин GoIP
    - GOIP_PASS=admin             # Пароль GoIP
    - GOIP_SSL=false              # Использовать HTTPS
```

### Проверка подключения

```bash
# Тест отправки SMS через GoIP
curl -X POST http://localhost:8000/api/sms/send \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "79991234567",
    "message": "Test",
    "sim_number": 1
  }'
```

**Ожидаемый ответ:**
```json
{
  "sms_id": 1,
  "status": "sent",
  "phone_number": "79991234567",
  "sim_number": 1,
  "goip_response": "send success"
}
```

---

## 💡 Советы по использованию:

### Длина СМС

- **1 СМС (латиница):** до 160 символов
- **1 СМС (кириллица):** до 70 символов
- **Длинные СМС:** разбиваются на части (списывается за каждую)

### Оптимизация текста

**✅ Хорошо:**
```
Напоминаем о записи 15.12 в 10:00
Клиника "Здоровье"
Тел: 8-800-123-4567
```

**❌ Плохо:**
```
Уважаемый клиент! Позвольте напомнить Вам о том, что у Вас запланирован визит...
[180 символов = 3 СМС]
```

### Когда использовать каждый тип:

| Сценарий | Рекомендуемый тип |
|----------|-------------------|
| Напоминание о записи | Звонок + СМС при недозвоне |
| Акция/скидка | Звонок + СМС при успехе (промокод) |
| Уведомление о доставке | Только СМС |
| Опрос/голосование | Только звонок |
| Подтверждение заказа | Звонок + СМС (оба варианта) |

---

## 📈 Статистика SMS:

В карточке кампании теперь отображается:

- **SMS отправлено** - количество успешно отправленных СМС
- **SMS ошибки** - количество неудачных попыток
- **SMS при недозвоне** - сколько отправлено при недозвоне
- **SMS при успехе** - сколько отправлено после успешного звонка

---

## 🎬 Готово!

Теперь у вас есть полноценная система для:

✅ Голосовых звонков
✅ SMS-рассылок
✅ Комбинированных кампаний
✅ Управления шаблонами
✅ Гибких сценариев отправки

**Откройте http://localhost:8000 и начните работу!**
