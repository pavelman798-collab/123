# Система исходящих телефонных кампаний с GoIP-4

Автоматизированная система для управления исходящими звонками через GoIP-4 GSM шлюз с функциями антидетекта и умного подбора SIM-карт.

## 🎯 Возможности

- ✅ Подключение GoIP-4 (4 SIM-карты)
- ✅ Умный подбор SIM по оператору абонента (бесплатные внутрисетевые звонки)
- ✅ Антидетект-система (рандомные интервалы, ротация SIM)
- ✅ Отправка SMS через GoIP
- ✅ Веб API для управления кампаниями
- ✅ PostgreSQL база данных с историей звонков
- ✅ Asterisk PBX для маршрутизации звонков

## 📋 Требования

- Mac M1/M2/M3 с macOS
- Docker Desktop установлен
- GoIP-4 устройство
- 4 SIM-карты разных операторов (МТС, Билайн, Мегафон, Теле2)
- USB-C Ethernet адаптер (для подключения GoIP к Mac)

## 🚀 Быстрый старт

### 1. Подключение GoIP-4

**Вариант A: Через роутер (рекомендуется)**
```
Роутер
├── Mac M1 (WiFi) - 192.168.1.10
└── GoIP-4 (Ethernet) - 192.168.1.100
```

**Вариант B: Напрямую к Mac**
```
iPhone (Personal Hotspot)
   ↓
Mac M1 (Internet Sharing включен)
   ↓
USB-C Ethernet адаптер
   ↓
GoIP-4
```

### 2. Настройка GoIP-4

1. Подключи GoIP к сети
2. Найди IP адрес GoIP (обычно 192.168.1.100)
3. Открой в браузере: `http://192.168.1.100`
4. Логин/пароль: `admin/admin`
5. Вставь 4 SIM-карты в GoIP
6. Настрой SIP для каждой SIM:
   - SIM1 → порт 5060
   - SIM2 → порт 5061
   - SIM3 → порт 5062
   - SIM4 → порт 5063

### 3. Настройка Docker системы

**Шаг 1: Обнови IP адреса в конфигурации**

Отредактируй `docker/asterisk/etc/pjsip.conf`:

```bash
# Замени YOUR_MAC_IP на реальный IP твоего Mac
# Замени 192.168.1.100 на реальный IP GoIP

# Узнать IP Mac:
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Шаг 2: Запусти Docker контейнеры**

```bash
cd ~/path/to/project

# Запуск всех сервисов
docker compose up -d

# Проверка статуса
docker compose ps

# Логи
docker compose logs -f
```

**Шаг 3: Проверь что всё работает**

```bash
# Проверка Asterisk
docker exec -it phone-asterisk asterisk -rx "pjsip show endpoints"

# Должно показать 4 endpoint'а: goip-sim1, goip-sim2, goip-sim3, goip-sim4

# Проверка базы данных
docker exec -it phone-postgres psql -U phone_user -d phone_campaigns -c "SELECT * FROM sim_cards;"

# Проверка API
curl http://localhost:8000/
```

### 4. Настройка SIM-карт в базе

После подключения GoIP обнови информацию о SIM-картах:

```bash
# Подключись к PostgreSQL
docker exec -it phone-postgres psql -U phone_user -d phone_campaigns

# Обнови данные SIM-карт
UPDATE sim_cards SET
    operator = 'МТС',
    phone_number = '79991234567',  -- Реальный номер SIM
    status = 'active'
WHERE sim_number = 1;

UPDATE sim_cards SET
    operator = 'Билайн',
    phone_number = '79991234568',
    status = 'active'
WHERE sim_number = 2;

UPDATE sim_cards SET
    operator = 'Мегафон',
    phone_number = '79991234569',
    status = 'active'
WHERE sim_number = 3;

UPDATE sim_cards SET
    operator = 'Теле2',
    phone_number = '79991234570',
    status = 'active'
WHERE sim_number = 4;

\q
```

## 📞 Использование

### Создание кампании через API

```bash
# 1. Создать кампанию
curl -X POST http://localhost:8000/campaigns \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовая кампания",
    "description": "Обзвон клиентов",
    "audio_file": "message.wav"
  }'

# Ответ: {"campaign_id": 1, "status": "created"}

# 2. Загрузить список номеров (CSV файл)
# Создай файл numbers.csv:
# phone_number
# 79991234567
# 79991234568

curl -X POST http://localhost:8000/campaigns/1/numbers \
  -F "file=@numbers.csv"

# 3. Запустить кампанию
curl -X POST http://localhost:8000/campaigns/1/start

# 4. Проверить статистику
curl http://localhost:8000/campaigns/1/stats
```

### Тестовый звонок

```bash
# Один звонок на номер
curl -X POST http://localhost:8000/call \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "79991234567",
    "audio_file": "test.wav"
  }'
```

### Просмотр статуса SIM-карт

```bash
# Статус всех SIM
curl http://localhost:8000/sims

# Ответ покажет:
# - Сколько звонков сегодня с каждой SIM
# - Сколько звонков за последний час
# - Статус (active/blocked/disabled)
```

## 🎭 Антидетект функции

Система автоматически использует антидетект-алгоритмы:

### 1. Рандомизация интервалов
- Базовая пауза: 45 сек - 3 мин
- 15% звонков с длинной паузой: 5-15 мин

### 2. Ротация SIM-карт
- Не более 80 звонков/день с одной SIM
- Не более 15 звонков/час с одной SIM
- Автоматическое переключение на другую SIM

### 3. Умный подбор оператора
- Определяет оператора абонента по префиксу
- Выбирает SIM того же оператора (бесплатный звонок)
- Если нет SIM нужного оператора - берет наименее загруженную

### 4. Лимиты настраиваются в БД

```sql
UPDATE sim_cards SET
    daily_call_limit = 100,  -- Звонков в день
    hourly_call_limit = 20   -- Звонков в час
WHERE sim_number = 1;
```

## 📱 Отправка SMS

GoIP-4 поддерживает SMS! Для отправки используй HTTP API GoIP:

```python
import requests

# Отправка SMS
url = "http://192.168.1.100/default/en_US/send.html"
data = {
    'username': 'admin',
    'password': 'admin',
    'line': '1',             # Номер SIM (1-4)
    'telnum': '79991234567', # Кому
    'msg': 'Привет!'         # Текст
}

response = requests.post(url, data=data)
print(response.text)
```

## 🔧 Настройка и управление

### Просмотр логов

```bash
# Логи Asterisk
docker compose logs asterisk

# Логи Campaign Manager
docker compose logs campaign-manager

# Логи PostgreSQL
docker compose logs postgres

# Логи в реальном времени
docker compose logs -f
```

### Остановка системы

```bash
# Остановить все контейнеры
docker compose stop

# Остановить и удалить
docker compose down

# Остановить и удалить с очисткой volumes
docker compose down -v
```

### Перезапуск

```bash
# Перезапустить один сервис
docker compose restart asterisk

# Перезапустить всё
docker compose restart
```

## 📊 Структура проекта

```
.
├── docker-compose.yml          # Главная конфигурация Docker
├── docker/
│   ├── asterisk/
│   │   ├── etc/                # Конфиги Asterisk
│   │   │   ├── pjsip.conf      # SIP настройки для GoIP
│   │   │   ├── extensions.conf # Диалплан
│   │   │   ├── manager.conf    # AMI
│   │   │   └── rtp.conf        # RTP (аудио)
│   │   └── agi/
│   │       └── select_sim.py   # AGI скрипт выбора SIM
│   ├── postgres/
│   │   └── init.sql            # Схема БД
│   └── scripts/
│       ├── Dockerfile
│       ├── requirements.txt
│       └── campaign_manager.py # API сервер
├── audio/                      # Аудиофайлы для проигрывания
└── logs/                       # Логи звонков
```

## 🐛 Troubleshooting

### Asterisk не видит GoIP

```bash
# Проверь подключение
docker exec -it phone-asterisk asterisk -rx "pjsip show endpoints"

# Если Not in use - проверь:
# 1. IP адрес GoIP правильный в pjsip.conf
# 2. GoIP доступен по сети: ping 192.168.1.100
# 3. SIP настроен в GoIP веб-интерфейсе
```

### Звонки не идут

```bash
# Проверь SIM-карты в GoIP
# Открой http://192.168.1.100
# Раздел: System Status → SIM Status

# Проверь лимиты в БД
docker exec -it phone-postgres psql -U phone_user -d phone_campaigns \
  -c "SELECT * FROM sim_cards;"

# Если calls_today >= daily_call_limit - сбрось счетчики:
UPDATE sim_cards SET calls_today = 0, calls_this_hour = 0;
```

### База данных не запускается

```bash
# Проверь логи
docker compose logs postgres

# Пересоздай контейнер
docker compose down
docker compose up -d postgres
```

## ⚠️ Важно

1. **Легальность**: Используй только для легальных звонков с согласия абонентов
2. **Операторы блокируют**: Массовые звонки могут привести к блокировке SIM
3. **Тарифы**: Убедись что на SIM подключен тариф с бесплатными внутрисетевыми
4. **Антидетект**: Не злоупотребляй - больше пауз = меньше блокировок

## 📞 API Документация

После запуска открой в браузере:
```
http://localhost:8000/docs
```

Интерактивная документация API (Swagger UI)

## 🎓 Следующие шаги

1. Купи GoIP-4 (~$200-300)
2. Купи 4 SIM-карты разных операторов
3. Настрой систему по этой инструкции
4. Протестируй на 10-20 звонках
5. Масштабируй до нужных объемов

---

**Готово!** Система настроена и готова к использованию. 🚀
